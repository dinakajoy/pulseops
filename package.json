{
  "name": "pulseops",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.{ts,tsx}": [
      "npx --prefix backend eslint --fix",
      "npx --prefix backend prettier --write"
    ],
    "backend/**/*.{json,md,yml,yaml}": [
      "npx --prefix backend prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0"
  }
}
